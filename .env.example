# Server
PORT=3000

# Hostinger MySQL (hPanel → Databases → MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=your_mysql_user
DB_PASSWORD=your_mysql_password
DB_NAME=your_database_name

# CRM admin dashboard — set a strong password
CRM_ADMIN_PASSWORD=change-this-to-a-strong-password
